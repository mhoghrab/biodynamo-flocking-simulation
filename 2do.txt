// directions array static dings, dass das nur einmal berechnet werden muss und immer auf das verwiesen wird#

L2F functor nicht in der aktuellen version?

CalculateNeighborData auslagern

Normalize() function  falls norm 0

json parameter

OperationRegistry::GetInstance()->AddOperationImpl(
      "UpdateOp", OpComputeTarget::kCpu, new UpdateOp());

norm squared function

openloopdomain ktorus
