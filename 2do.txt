

  // max_dir_idx is the maximal index of the original directions_ that still is in
  // the viewing cone of a boid
  max_dir_idx = std::floor((directions.size() / 2) * (1 - cos_perception_angle));

  GetUnobstructedDirection best_obstructed_direction


//////////////////////////////////////////////////
Flocking2

paraview export of obstacles

SteerTowards?

phi_h_a in eine funktion

  b_ik : im moment /r_b 
  idea : schon vorher velocity matchen um auszuweichen
  wenn ich wegfliege vernachlässigen, und wenn <d_b normal

  solve: how can boids catch up to flock